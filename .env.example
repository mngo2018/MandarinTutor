# Optional. When set, the tutor uses OpenAI for conversational replies.
# Without it, the app runs in offline practice mode with built-in lessons.
OPENAI_API_KEY=

# Optional: override the chat model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
